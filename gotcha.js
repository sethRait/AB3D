var user = form.elements["user"];
var kill = form.elements["kill"];

/*
** Adds one point to user score. Removes kill from user target list and replaces it with a random target in the least-targetted pool.
*/
function randomized_newtarget(user, kill){
		
}

